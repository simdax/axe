Quarks.install("https://github.com/simdax/axe");

//recompile 

API.mountDuplexOSC

//cette fois ci load "trucouf"
a=API.load("test")

a.async("new");

API =>
"boot"
"go"
"up" => avec une val
"explosionFatale"
